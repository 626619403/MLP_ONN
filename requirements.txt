# Core dependencies for the main MNIST training pipeline and `noise_scan.py`.
# Use the plain PyPI wheels by default.
torch==2.0.0
torchvision==0.15.1
matplotlib==3.7.1

# Optional analysis dependencies used by
# `backward-gradient-5bit-noise-simulation.ipynb`.
numpy==1.24.2
scikit-learn==1.2.2

# Notes:
# - If you specifically need CUDA 11.8 wheels, install PyTorch separately:
#   pip install torch==2.0.0+cu118 torchvision==0.15.1+cu118 --index-url https://download.pytorch.org/whl/cu118
# - The bundled `quantized_pytorch_master/` directory is archival/reference code
#   and may require its own separate environment and dependencies.
